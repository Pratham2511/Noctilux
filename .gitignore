# Verbis workspace — commit most of .qmind/ to version control
# Only exclude sensitive encrypted files and logs
.qmind/priv_map.enc
.qmind/pii_audit.log
.qmind/*.log

# Python
python_backend/venv/
python_backend/__pycache__/
python_backend/**/__pycache__/
python_backend/*.pyc
.env

# Build outputs (these are kept)
# DO NOT add: out/ webview/dist/ media/
node_modules/
*.vsix
