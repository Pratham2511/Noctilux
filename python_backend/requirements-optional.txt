# Optional — install only if user enables local embedding / PII features
# These require significant disk space (sentence-transformers pulls PyTorch ~500MB)

sentence-transformers>=2.7.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
