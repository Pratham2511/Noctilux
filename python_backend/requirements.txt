# QueryMind Python Backend — pinned dependencies
# Python 3.11+ required.

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9        # PostgreSQL driver
PyMySQL==1.1.1                # MySQL driver
pymongo==4.7.3                # MongoDB driver

# SQL parsing & AST
sqlglot==23.10.0              # Syntax validation + AST transformation

# RAG / vector store
chromadb==0.5.3
sentence-transformers==3.0.1   # all-MiniLM-L6-v2 embeddings (local)

# LLM clients
openai==1.35.0                # Used for cloud LLM API (Llama-3 via OpenAI-compatible endpoint)
httpx==0.27.0                 # For Ollama REST calls

# Privacy & security
cryptography==42.0.7          # AES-256-GCM for priv_map.enc
presidio-analyzer==2.2.355    # PII detection
presidio-anonymizer==2.2.355  # PII masking

# Plan similarity
apted==1.0.3                  # Tree-edit-distance algorithm

# Data manipulation
pandas==2.2.2                 # Federated query merge

# Utilities
python-multipart==0.0.9
PyYAML==6.0.1
loguru==0.7.2

# Testing (dev only, but listed for reproducibility)
pytest==8.2.2
pytest-asyncio==0.23.7
respx==0.21.1
