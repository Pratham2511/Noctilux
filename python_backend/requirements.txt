# Lumina Python Backend — dependencies
# Python 3.11+ required.

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0          # PostgreSQL driver
pymysql>=1.1.0                  # MySQL driver
pymongo>=4.6.0                  # MongoDB driver

# SQL parsing & AST
sqlglot>=23.0.0                 # Syntax validation + AST transformation

# RAG / vector store
chromadb>=0.4.0
sentence-transformers>=2.7.0    # all-MiniLM-L6-v2 embeddings (local)

# LLM clients
openai>=1.0.0                   # OpenAI-compatible client — used for Gemini, Groq, and Ollama
httpx>=0.27.0                   # Async HTTP client (legacy Ollama REST calls)

# Privacy & security
cryptography>=42.0.0            # AES-256-GCM for priv_map.enc
presidio-analyzer>=2.2.0        # PII detection
presidio-anonymizer>=2.2.0      # PII masking

# Plan similarity
apted>=1.0.3                    # Tree-edit-distance algorithm

# Data manipulation
pandas>=2.2.0                  # Federated query merge

# Utilities
python-multipart>=0.0.9
PyYAML>=6.0
loguru>=0.7.2

# Testing (dev only, but listed for reproducibility)
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0
